/*const insertCostumerOpt = (user, token) =>{
    let options = {
        method: 'POST',
        url: `https://api.moloni.pt/v1/products/getByName/?access_token=${token}&json=true`,
        headers: {'Content-Type' : 'application/json'},
        body: JSON.stringify(user)
    }
    return options;
}

function clientOpt(client){
    let user = {
        company_id : 128348,
        vat : user.nif,
        number : 1,
        name : 
    }
}*/